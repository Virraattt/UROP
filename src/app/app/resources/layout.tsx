import React from "react";

export const metadata = {
  title: "Resources | AcademiaX",
};

export default function ResourcesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
