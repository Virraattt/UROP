import React from "react";

export const metadata = {
  title: "Contribute | AcademiaX",
};

export default function ContributeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
